import { useState } from "react";
import { Mail, MapPin, Phone } from "lucide-react";
import { Button } from "@/components/ui/button";
import { academy, navItems, whatsappUrl } from "@/data/content";
import { SectionHeading, WhatsAppButton, LogoLockup } from "./shared";

export function ContactFooter() {
  const [sent, setSent] = useState(false);
  const [errors, setErrors] = useState<Record<string, string>>({});
  const validate = (form: HTMLFormElement) => { const data = new FormData(form); const next: Record<string, string> = {}; if (!String(data.get("studentName") ?? "").trim()) next.studentName = "Enter the student's name."; if (!String(data.get("parentName") ?? "").trim()) next.parentName = "Enter the parent's name."; if (!/^[6-9]\d{9}$/.test(String(data.get("phone") ?? "").replace(/\D/g, ""))) next.phone = "Enter a 10-digit phone number."; if (!String(data.get("class") ?? "")) next.class = "Select a class."; setErrors(next); return Object.keys(next).length === 0; };
  return <>
    <section id="contact" className="scroll-mt-24 px-6 py-20 sm:py-28"><div className="mx-auto grid max-w-[1200px] gap-14 lg:grid-cols-2 lg:gap-20"><div><SectionHeading eyebrow="Get In Touch" title="Come See The" highlight="Classroom" intro="Call, message or visit us. We will help you find the right batch for your child." /><div className="mt-9 space-y-6"><ContactRow icon={<Phone />} label="Phone / WhatsApp"><a href={academy.phoneHref} className="hover:text-gold">{academy.phone}</a><a href={whatsappUrl} target="_blank" rel="noreferrer" className="ml-3 inline-flex rounded-full border border-gold/30 px-3 py-1 text-xs text-gold">WhatsApp</a></ContactRow><ContactRow icon={<Mail />} label="Email"><a href={`mailto:${academy.email}`} className="hover:text-gold">{academy.email}</a></ContactRow><ContactRow icon={<MapPin />} label="Address"><span>{academy.address}</span></ContactRow><ContactRow icon={<span className="font-display text-lg">◷</span>} label="Class Timings"><span>{academy.timings}</span></ContactRow></div><div className="mt-8 overflow-hidden rounded-2xl border border-gold/15 bg-ink-soft"><iframe title="Harshika Academy location map" src={academy.mapUrl} loading="lazy" className="h-64 w-full border-0 grayscale-[60%] contrast-125" /></div><Button asChild variant="outline" className="mt-4 rounded-full border-gold/30 bg-transparent text-gold hover:bg-gold/10 hover:text-gold"><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(academy.address)}`} target="_blank" rel="noreferrer">Get Directions</a></Button><div className="mt-8 flex gap-5 font-utility text-xs font-semibold uppercase tracking-wider text-muted-copy">{academy.social.map((item) => <a key={item.label} href={item.href} target="_blank" rel="noreferrer" className="hover:text-gold">{item.label}</a>)}</div></div><div className="rounded-2xl border border-gold/15 bg-ink-soft p-6 sm:p-9"><h3 className="font-display text-3xl uppercase text-paper">Send An Enquiry</h3>{sent ? <div className="mt-8 border-l-2 border-gold pl-5"><p className="font-display text-2xl uppercase text-gold">Enquiry sent.</p><p className="mt-2 text-sm text-muted-copy">We'll call you within a day.</p><Button variant="link" className="mt-3 px-0 text-gold" onClick={() => setSent(false)}>Send another enquiry</Button></div> : <form className="mt-8 space-y-5" onSubmit={(event) => { event.preventDefault(); if (validate(event.currentTarget)) setSent(true); }} noValidate><Field id="studentName" label="Student Name" error={errors.studentName} /><Field id="parentName" label="Parent's Name" error={errors.parentName} /><div className="grid gap-5 sm:grid-cols-2"><Field id="phone" label="Phone Number" type="tel" error={errors.phone} /><div><label htmlFor="class" className="font-utility text-[10px] font-semibold uppercase tracking-wider text-gold/80">Class</label><select id="class" name="class" defaultValue="" className="mt-2 h-11 w-full rounded-lg border border-gold/20 bg-ink px-3 text-sm text-paper outline-none focus:border-gold focus:ring-2 focus:ring-gold/30"><option value="">Select class</option><option>Class 6–8</option><option>Class 9–10</option><option>Class 11–12</option><option>Competitive Exams</option></select>{errors.class && <p className="mt-1 text-xs text-gold-bright">{errors.class}</p>}</div></div><Field id="subjects" label="Subject(s) of interest" /><div><label htmlFor="message" className="font-utility text-[10px] font-semibold uppercase tracking-wider text-gold/80">Message (Optional)</label><textarea id="message" name="message" rows={4} className="mt-2 w-full resize-none rounded-lg border border-gold/20 bg-ink px-3 py-3 text-sm text-paper outline-none focus:border-gold focus:ring-2 focus:ring-gold/30" /></div><Button type="submit" className="h-12 w-full rounded-full bg-gold font-utility text-xs font-bold uppercase tracking-widest text-ink hover:bg-gold-bright">Send Enquiry</Button><p className="text-center text-xs text-muted-copy">In a hurry? <a href={whatsappUrl} target="_blank" rel="noreferrer" className="font-semibold text-gold hover:text-gold-bright">Message us directly on WhatsApp.</a></p></form>}</div></div></section>
    <section className="relative overflow-hidden border-y border-gold/20 bg-ink px-6 py-20 text-center"><div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_100%,oklch(0.76_0.16_82_/_12%),transparent_55%)]" /><div className="relative mx-auto max-w-3xl"><h2 className="font-display text-4xl uppercase leading-tight text-paper sm:text-5xl">Seats Fill Fast. <span className="gold-foil">Talk To Us Today.</span></h2><p className="mt-4 text-base text-muted-copy">Book a free demo class and see how we teach before you decide.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><WhatsAppButton /><Button asChild variant="outline" className="h-11 rounded-full border-gold/30 bg-transparent px-6 text-xs font-bold uppercase tracking-wider text-paper hover:bg-gold/10 hover:text-paper"><a href={academy.phoneHref}>Call Now</a></Button></div></div></section>
    <footer className="border-t border-gold/15 bg-ink px-6 py-14"><div className="mx-auto grid max-w-[1200px] gap-12 md:grid-cols-3"><div><LogoLockup /><p className="mt-6 max-w-xs text-sm leading-7 text-muted-copy">Building Knowledge. Shaping Futures. A local academy for clear concepts, steady practice and honest guidance.</p></div><div><h3 className="font-utility text-xs font-semibold uppercase tracking-widest text-gold">Explore</h3><div className="mt-5 grid grid-cols-2 gap-3 text-sm text-muted-copy">{navItems.map((item) => <a key={item.href} href={item.href} className="hover:text-gold">{item.label}</a>)}</div></div><div><h3 className="font-utility text-xs font-semibold uppercase tracking-widest text-gold">Reach Us</h3><p className="mt-5 text-sm leading-7 text-muted-copy">{academy.address}<br /><a href={academy.phoneHref} className="hover:text-gold">{academy.phone}</a><br /><a href={`mailto:${academy.email}`} className="hover:text-gold">{academy.email}</a></p><div className="mt-4 flex gap-4 text-xs text-muted-copy">{academy.social.map((item) => <a key={item.label} href={item.href} target="_blank" rel="noreferrer" className="hover:text-gold">{item.label}</a>)}</div></div></div><div className="mx-auto mt-12 flex max-w-[1200px] flex-col justify-between gap-3 border-t border-gold/10 pt-6 text-[10px] uppercase tracking-wider text-muted-copy sm:flex-row"><span>© 2026 Harshika Academy. All rights reserved.</span><span>Quality Education · Strong Foundation · Bright Future</span></div></footer>
  </>;
}
function ContactRow({ icon, label, children }: { icon: React.ReactNode; label: string; children: React.ReactNode }) { return <div className="flex gap-4"><div className="mt-1 text-gold">{icon}</div><div className="min-w-0"><p className="font-utility text-[10px] font-semibold uppercase tracking-widest text-gold/80">{label}</p><div className="mt-1 text-sm leading-6 text-muted-copy">{children}</div></div></div>; }
function Field({ id, label, type = "text", error }: { id: string; label: string; type?: string; error?: string }) { return <div><label htmlFor={id} className="font-utility text-[10px] font-semibold uppercase tracking-wider text-gold/80">{label}</label><input id={id} name={id} type={type} className="mt-2 h-11 w-full rounded-lg border border-gold/20 bg-ink px-3 text-sm text-paper outline-none focus:border-gold focus:ring-2 focus:ring-gold/30" />{error && <p className="mt-1 text-xs text-gold-bright">{error}</p>}</div>; }
